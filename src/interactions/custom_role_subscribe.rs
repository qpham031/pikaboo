use anyhow::Result;
use twilight_model::http::interaction::InteractionResponse;

use crate::core::app_state::AppState;

pub async fn run(state: AppState, status: bool) -> Result<InteractionResponse> {
    todo!()
}
